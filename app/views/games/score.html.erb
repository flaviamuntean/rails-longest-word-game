<h1>Result</h1>

<% if @result["found"] != true %>
  <p>Sorry but <strong><%=@word.upcase %></strong> does not seem to be a valid English word...! </p>
<% elsif @attempt_arr.all? { |e| params[:letters].include?(e) } && @attempt_arr.all? { |e| @attempt_arr.count(e) <= params[:letters].count(e) } %>
  <p><strong>Congratulations!</strong> <%=@word.upcase %> is a valid English word!</p>
  <% session[:score] += @word.length %>
<% else %>
  <p>Sorry but <strong><%=@word.upcase %></strong> can't be built out of <%=@letters %></p>
<% end %>

<h2>Score: <%= session[:score] %></h2>


<%= link_to 'Play again', new_path %>
